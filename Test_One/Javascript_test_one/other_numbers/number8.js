// creating an object that store the information about the movie
const movie = {
    title: "Passion for Christ",
    duration: 60,
    star: {
        name: "Wonder Jones",
        country: "American",
        age: "45years"
    }
};

/// creating a function that declares the output
// console.log(movie.title + "lasts for", movie.duration. move.star.name + "is", movie.star.age + "and born in", movie.star.country)
console.log(movie.title + " lasts for " + movie.duration + " minutes. " + movie.star.name + " is " + movie.star.age + " and born in " + movie.star.country + ".");
